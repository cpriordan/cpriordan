name: Fin Regression UI Tests — STG (post JS deploy)

# ── Triggers ──────────────────────────────────────────────────────────────────
#
# Primary : repository_dispatch — fired by the JS deploy workflow in
#           github.com/Finalytics-ai/fin-personalization-js after a successful
#           STG deploy. The JS workflow must include a step like:
#
#   - name: Trigger UI tests
#     uses: peter-evans/repository-dispatch@v3
#     with:
#       token: ${{ secrets.TESTS_REPO_PAT }}
#       repository: <this-repo>
#       event-type: js-deploy-stg-success
#       client-payload: '{"env": "stg"}'
#
#   TESTS_REPO_PAT must be a PAT with `repo` scope stored as a secret in the
#   fin-personalization-js repo.
#
# Fallback: workflow_dispatch — manual run from the Actions tab.
#
on:
  repository_dispatch:
    types: [js-deploy-stg-success]

  workflow_dispatch:
    inputs:
      env:
        description: Target environment
        required: true
        type: choice
        options: [stg, prod]
        default: stg

# Cancel in-flight runs for the same deploy event so a rapid redeploy
# doesn't queue up duplicate test runs.
concurrency:
  group: fin-ui-tests-${{ github.event.client_payload.env || github.event.inputs.env || github.run_id }}
  cancel-in-progress: true

# ═══════════════════════════════════════════════════════════════════════════════
# PYTEST-BASED CLIENTS  (run as one matrix job — each entry is its own job)
# ═══════════════════════════════════════════════════════════════════════════════
jobs:
  pytest-suite:
    name: ${{ matrix.display }} (${{ github.event.client_payload.env || github.event.inputs.env || 'stg' }})
    runs-on: ubuntu-22.04
    timeout-minutes: 20

    # Only run when the deploy workflow succeeded (or when triggered manually).
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'repository_dispatch'

    env:
      TEST_ENV: ${{ github.event.client_payload.env || github.event.inputs.env || 'stg' }}
      HEADLESS: "true"
      DEFAULT_TIMEOUT: "40000"

    strategy:
      fail-fast: false   # one client failing must NOT cancel the others
      matrix:
        include:

          - client: gocu
            display: "GOCU — Youth Checking"
            tests: >-
              fin-ui-tests/test_gocu_youthchecking_stgtags_and_js_errors_search.py
            screenshots: screenshots_gocu_using_pytest/

          - client: swfcu
            display: "SWFCU — RV/Boat Loan"
            tests: >-
              fin-ui-tests/test_swfcu_rvboatloan_stgtags_and_js_errors_search.py
            screenshots: screenshots_swfcu_using_pytest/

          - client: tru
            display: "TRU — Rates"
            tests: >-
              fin-ui-tests/test_tru_rates_page_js_errors.py
            screenshots: screenshots_tru_using_pytest/

          - client: tru-multiproduct
            display: "TRU — Multiproduct + Rates"
            tests: >-
              fin-ui-tests/test_tru_multiproduct_stgtags_and_js_errors_search.py
            screenshots: screenshots_tru_using_pytest/

          - client: u1st
            display: "U1st — Personal Loans"
            tests: >-
              fin-ui-tests/test_u1st_personal_stgtags_and_js_errors_search.py
            screenshots: screenshots_u1st_using_pytest/

          - client: ssscu
            display: "SSSCU — Credit Cards"
            tests: >-
              fin-ui-tests/test_ssscu_creditcards_stgtags_and_js_errors_search.py
            screenshots: screenshots_ssscu_using_pytest/

          - client: glbl
            display: "Global — Money Market"
            tests: >-
              fin-ui-tests/test_global_mma_stgtags_and_js_errors_search.py
            screenshots: screenshots_glbl_using_pytest/

          - client: ad-expiration
            display: "Ad Expiration (time + views)"
            tests: >-
              fin-ui-tests/test_ad_expiration_by_time.py
              fin-ui-tests/test_ad_expiration_by_aggressive_expiration_by_time.py
              fin-ui-tests/test_ad_expiration_by_views.py
            screenshots: screenshots_*expiration*_using_pytest/

          - client: nymcu-checking-search
            display: "Municipal — Checking Search"
            tests: >-
              fin-ui-tests/test_nymcu_checking_search_STG.py
            screenshots: screenshots_nymcu_checking_search_using_pytest/

          - client: langley-certificate-search
            display: "Langley — Certificate Search"
            tests: >-
              fin-ui-tests/test_langley_certificate_search_STG.py
            screenshots: screenshots_langley_certificate_search_using_pytest/

          - client: uhfcu-carloan-search
            display: "UHFCU — Car Loan Search"
            tests: >-
              fin-ui-tests/test_uhfcu_carloan_search_STG.py
            screenshots: screenshots_uhfcu_carloan_search_using_pytest/

    steps:
      # ── Checkout ─────────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      # ── Python ───────────────────────────────────────────────────────────────
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pytests/requirements.txt

      # ── Dependencies ─────────────────────────────────────────────────────────
      - name: Install Python dependencies
        run: pip install -r pytests/requirements.txt pytest-html

      - name: Install Playwright browsers
        run: playwright install --with-deps chromium

      # ── Pre-flight info ───────────────────────────────────────────────────────
      - name: Display trigger info
        run: |
          echo "Client      : ${{ matrix.client }}"
          echo "Environment : ${{ env.TEST_ENV }}"
          echo "Trigger     : ${{ github.event_name }}${{ github.event.action != '' && format(' ({0})', github.event.action) || '' }}"
          echo "Branch      : ${{ github.ref_name }}"

      # ── Run tests ─────────────────────────────────────────────────────────────
      - name: Run ${{ matrix.display }} tests
        id: run_tests
        run: |
          pytest ${{ matrix.tests }} \
            -v --tb=short \
            --junitxml=${{ matrix.client }}_results.xml \
            --html=${{ matrix.client }}_report.html --self-contained-html \
            -p no:warnings
        continue-on-error: true

      # ── Step summary (visible immediately when this job finishes) ─────────────
      - name: Write step summary
        if: always()
        run: |
          echo "## ${{ matrix.display }} — ${{ env.TEST_ENV }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.run_tests.outcome }}" == "success" ]; then
            echo "✅ **PASSED**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **FAILED** — download the HTML report artifact for details" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Triggered by: \`${{ github.event_name }}\` on \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY

      # ── Artifacts ─────────────────────────────────────────────────────────────
      - name: Upload HTML report + JUnit XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.client }}-report-${{ env.TEST_ENV }}
          path: |
            ${{ matrix.client }}_report.html
            ${{ matrix.client }}_results.xml
          retention-days: 14

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.client }}-screenshots-${{ env.TEST_ENV }}
          path: ${{ matrix.screenshots }}
          retention-days: 7
          if-no-files-found: warn

      # ── Propagate failure ─────────────────────────────────────────────────────
      - name: Fail job if tests failed
        if: steps.run_tests.outcome == 'failure'
        run: exit 1


# ═══════════════════════════════════════════════════════════════════════════════
# LISTERHILL — standalone script (not pytest)
# ═══════════════════════════════════════════════════════════════════════════════
  listerhill:
    name: Listerhill — IRA Scenario (${{ github.event.client_payload.env || github.event.inputs.env || 'stg' }})
    runs-on: ubuntu-22.04
    timeout-minutes: 20

    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'repository_dispatch'

    env:
      TEST_ENV: ${{ github.event.client_payload.env || github.event.inputs.env || 'stg' }}
      HEADLESS: "true"
      DEFAULT_TIMEOUT: "40000"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pytests/requirements.txt

      - name: Install Python dependencies
        run: pip install -r pytests/requirements.txt

      - name: Install Playwright browsers
        run: playwright install --with-deps chromium

      - name: Run Listerhill IRA search
        id: run_tests
        working-directory: fin-ui-tests
        run: python test_listerhill_ira_search.py
        continue-on-error: true

      - name: Write step summary
        if: always()
        run: |
          echo "## Listerhill — IRA Scenario (${{ env.TEST_ENV }})" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.run_tests.outcome }}" == "success" ]; then
            echo "✅ **PASSED**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **FAILED** — check job logs for scenario-level details" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: listerhill-screenshots-${{ env.TEST_ENV }}
          path: fin-ui-tests/screenshots_listerhill/
          retention-days: 7
          if-no-files-found: warn

      - name: Fail job if tests failed
        if: steps.run_tests.outcome == 'failure'
        run: exit 1
